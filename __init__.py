# Package marker — Nautobot imports this synced repo as `rafay_nvcm_catalog.*`; without this file the
# import fails and the git sync registers ZERO jobs while reporting success (seen live, 2026-08-12).
